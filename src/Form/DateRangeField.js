import React from 'react';

const DateRangeField = () => {
  return (
    <div>
      <button type="button">Starting (Calendar)</button>
      <button type="button">Ending (Calendar)</button>
    </div>
  );
};

export default DateRangeField;
